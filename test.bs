import std

proc main () then
    std.printf("Hello, world!\n")

    random.callback(proc cb() then std.printf("hello from callback") end)

    for i = 0 to 10 then
        let z = i * 2
        printf("{}", z)
    end
end
import std
import math

proc main () then
    let msg = "Hello, world!\n"
    std.printval(msg)
    msg = "Hello, test message!\n"
    builtin.printval(msg)


    # random.callback(proc cb() then std.printf("hello from callback") end)
    # 
    # for i = 0 to 10 then
    #     let z = i * 2
    #     printf("{}", z)
    # end
end